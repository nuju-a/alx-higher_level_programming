include "list.h"

/**
  *is_palindrome - function checks if a singly linked list is apalindrome
  *@listint_t: singly linked list
  *@head: pointer to the first element in listint_t
  *
  *Return: 0 or 1
  */
int is_palindrome(listint_t **head)
{
	if (*head == NULL)
		return(1);

}
